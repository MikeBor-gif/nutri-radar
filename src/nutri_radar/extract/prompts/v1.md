Parse the food ingredient list into structured data.

For each ingredient give its canonical name in ENGLISH, lowercase, and its kind.
List allergens separately. If the text is unreadable or truncated, set unreadable
to true.

Source language: {lang}
Ingredients: {text}
