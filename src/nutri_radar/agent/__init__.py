"""Слайс `agent`: агент с инструментами, написанный руками.

Без LangChain, LlamaIndex, CrewAI и аналогов — правило 7 брифа. Это
осознанная часть демонстрации: цикл «план → инструмент → наблюдение →
ответ» должен быть виден целиком и объясним построчно, а не спрятан
за фасадом фреймворка.

Слайс импортирует `retrieval` — единственное разрешённое исключение
из правила изоляции слайсов (`ARCHITECTURE.md`): инструмент
`vector_search` есть обёртка над поиском M5, и дублировать поиск
внутри агента значило бы получить две расходящиеся реализации.
"""
